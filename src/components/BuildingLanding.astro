---
interface Props {
	title: string;
	body: string;
}

const { title, body } = Astro.props;
---

<div class="flex flex-col justify-center h-screen text-center">
	<h1 class="text-6xl font-bold">{title}</h1>
	<p class="text-2xl mt-5 text-gray-700">{body}</p>
</div>